# dbt
target/
dbt_packages/
logs/
profiles.yml

# MCP credentials
.mcp.json

# Claude Code
.claude/memory/

# Environment
.env
.env.*
!.env.example

# Terraform (covered by terraform/.gitignore for subdirectories)

# Project config (project-specific values, not secrets but not shared)
project-config.yaml

# OS
.DS_Store
